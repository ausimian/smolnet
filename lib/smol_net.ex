defmodule SmolNet do
  @moduledoc """
  An OTP-friendly embedded network stack powered by
  [`smoltcp`](https://github.com/smoltcp-rs/smoltcp).

  This initial project skeleton establishes the supervision and native-code
  boundaries. Stack and socket APIs are added in later implementation phases.
  """
end
